#notify
  %p.notice= notice
  %p.alert= alert
#user-info
  - if login?
    %a{"data-panel" => "main", :href => logout_path, "data-ajax"=>"false"} Sign Out
  - else
    %a{:href=> "auth/facebook", "data-panel" => "main", "rel"=>"external"}  Sign In with Facebook (Test)
